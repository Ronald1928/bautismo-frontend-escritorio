/// <reference types="vite/client" />
/* interface BautismoAPI {
  guardarBautismoBD: (
    datos: BautismoDatos
  ) => Promise<{ message: string; id: number }>;
  generarPdf: (datos: BautismoDatos) => Promise<Buffer>;
  handleVistaPrevia: (datos: BautismoDatos) => Promise<Buffer>;
  buscar: (nombre: string) => Promise<Certificado[]>;
  descargarCertificado: (id: number) => Promise<Buffer>;
  checkIdDisponible: (
    libroBautizo: string,
    folioBautizo: string,
    numeroArchivo: string
  ) => Promise<boolean>;
  eliminarCertificado: (id: number) => Promise<{ message: string }>;
  obtenerTodos: () => Promise<Certificado[]>;
  actualizarBautismo: (
    id: number,
    datosActualizados: BautismoDatos
  ) => Promise<{ message: string }>;
  estadisticasBautismos: (anio: string) => Promise<{
    meses: { mes: string; total: number }[];
    totalAnual: number;
  }>;
}

interface Window {
  bautismoAPI: BautismoAPI;
} */
